# gh-rending-news

Nuxt project scaffolded with pnpm and UnoCSS.

Commands:

- pnpm install
- pnpm dev
- pnpm build
- pnpm start

UnoCSS is configured via unocss.config.ts and enabled in nuxt.config.ts via the @unocss/nuxt module.
